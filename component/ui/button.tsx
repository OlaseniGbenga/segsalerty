import { Button } from "@mantine/core";

const MyButton = () => {
  return (
    <Button style={{ borderRadius: "10px", background:"red", color:"blue" }} variant="filled">
      Button
    </Button>
  );
};

export default MyButton;
